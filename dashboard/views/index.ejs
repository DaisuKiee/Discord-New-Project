<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="page-title">Discord Bot - Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        html {
            overflow-x: hidden;
            width: 100%;
        }
        
        body {
            background: #030014;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            overflow-x: hidden;
            color: #fff;
            width: 100%;
            max-width: 100vw;
            position: relative;
        }
        
        /* Prevent horizontal scroll on mobile */
        @media (max-width: 1024px) {
            html, body {
                overflow-x: hidden !important;
                width: 100% !important;
                max-width: 100vw !important;
            }
            .orb { position: fixed !important; }
        }
        
        /* Welcome Preloader */
        .welcome-preloader {
            position: fixed;
            inset: 0;
            background: #030014;
            z-index: 9999;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: opacity 0.5s ease, visibility 0.5s ease;
        }
        .welcome-preloader.hidden {
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
        }
        .preloader-logo {
            width: 100px;
            height: 100px;
            border-radius: 24px;
            background: linear-gradient(135deg, #8b5cf6, #6366f1);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 2rem;
            animation: logoPulse 2s ease-in-out infinite;
            box-shadow: 0 0 60px rgba(139, 92, 246, 0.5);
        }
        .preloader-logo img {
            width: 80px;
            height: 80px;
            border-radius: 16px;
        }
        .preloader-logo i {
            font-size: 3rem;
            color: #fff;
        }
        @keyframes logoPulse {
            0%, 100% { transform: scale(1); box-shadow: 0 0 60px rgba(139, 92, 246, 0.5); }
            50% { transform: scale(1.05); box-shadow: 0 0 80px rgba(139, 92, 246, 0.7); }
        }
        .preloader-text {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #fff 0%, #a78bfa 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.5rem;
        }
        .preloader-subtext {
            color: rgba(255, 255, 255, 0.5);
            font-size: 0.95rem;
            margin-bottom: 2rem;
        }
        .preloader-bar {
            width: 200px;
            height: 4px;
            background: rgba(139, 92, 246, 0.2);
            border-radius: 2px;
            overflow: hidden;
        }
        .preloader-progress {
            height: 100%;
            background: linear-gradient(90deg, #8b5cf6, #ec4899);
            border-radius: 2px;
            width: 0%;
            animation: loadProgress 1.5s ease-out forwards;
        }
        @keyframes loadProgress {
            0% { width: 0%; }
            50% { width: 70%; }
            100% { width: 100%; }
        }
        
        /* 3D Canvas - Hidden on mobile by default */
        #canvas-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }
        
        /* Force hide canvas on touch devices and small screens */
        @media (max-width: 1024px), (hover: none), (pointer: coarse) {
            #canvas-container,
            #canvas-container canvas,
            #canvas-container * {
                display: none !important;
                visibility: hidden !important;
                opacity: 0 !important;
                width: 0 !important;
                height: 0 !important;
                pointer-events: none !important;
            }
        }
        
        /* Gradient Orbs */
        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.5;
            animation: float 8s ease-in-out infinite;
        }
        .orb-1 {
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.4) 0%, transparent 70%);
            top: -200px;
            right: -100px;
            animation-delay: 0s;
        }
        .orb-2 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(236, 72, 153, 0.3) 0%, transparent 70%);
            bottom: -150px;
            left: -100px;
            animation-delay: -4s;
        }
        .orb-3 {
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.3) 0%, transparent 70%);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation-delay: -2s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-30px) scale(1.05); }
        }
        
        /* Grid Background */
        .grid-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(139, 92, 246, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(139, 92, 246, 0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            z-index: 0;
        }
        
        /* Content */
        .content { position: relative; z-index: 10; }
        
        /* Navigation */
        .nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            padding: 1rem 2rem;
            background: rgba(3, 0, 20, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(139, 92, 246, 0.1);
        }
        
        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .nav-brand {
            display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
            color: #fff;
        }
        
        .nav-brand img {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            border: 2px solid rgba(139, 92, 246, 0.3);
        }
        
        .nav-brand span {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #fff 0%, #a78bfa 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .nav-links {
            display: flex;
            gap: 2rem;
        }
        
        .nav-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            position: relative;
        }
        
        .nav-links a:hover {
            color: #fff;
        }
        
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #8b5cf6, #ec4899);
            transition: width 0.3s;
        }
        
        .nav-links a:hover::after {
            width: 100%;
        }
        
        .btn-glow {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
            padding: 12px 28px;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            color: #fff;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
            box-shadow: 0 0 30px rgba(139, 92, 246, 0.3);
        }
        
        .btn-glow:hover {
            transform: translateY(-2px);
            box-shadow: 0 0 50px rgba(139, 92, 246, 0.5);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 120px 2rem 80px;
        }
        
        .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            width: 100%;
        }
        
        .hero-content {
            opacity: 0;
            transform: translateY(40px);
            animation: slideUp 1s ease forwards;
            animation-delay: 0.3s;
            display: flex;
            flex-direction: column;
        }
        
        @keyframes slideUp {
            to { opacity: 1; transform: translateY(0); }
        }
        
        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(139, 92, 246, 0.1);
            border: 1px solid rgba(139, 92, 246, 0.3);
            padding: 8px 16px;
            border-radius: 50px;
            font-size: 0.875rem;
            color: #a78bfa;
            margin-bottom: 1.5rem;
        }
        
        .hero-badge i {
            animation: pulse 2s ease-in-out infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .hero h1 {
            font-size: 4.5rem;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 1.5rem;
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 50%, #6366f1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-size: 200% auto;
            animation: gradientShift 3s ease infinite;
        }
        
        @keyframes gradientShift {
            0%, 100% { background-position: 0% center; }
            50% { background-position: 100% center; }
        }
        
        .hero p {
            font-size: 1.25rem;
            color: rgba(255, 255, 255, 0.6);
            line-height: 1.8;
            margin-bottom: 2rem;
            max-width: 500px;
        }
        
        .hero-buttons {
            display: flex;
            gap: 1rem;
            margin-bottom: 3rem;
            flex-wrap: wrap;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
            padding: 16px 32px;
            border-radius: 14px;
            font-weight: 600;
            font-size: 1.1rem;
            text-decoration: none;
            color: #fff;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s;
            box-shadow: 0 4px 30px rgba(139, 92, 246, 0.4);
        }
        
        .btn-primary:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 8px 50px rgba(139, 92, 246, 0.6);
        }
        
        .btn-secondary {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 16px 32px;
            border-radius: 14px;
            font-weight: 600;
            font-size: 1.1rem;
            text-decoration: none;
            color: #fff;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s;
        }
        
        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(139, 92, 246, 0.5);
            transform: translateY(-3px);
        }
        
        /* Stats */
        .hero-stats {
            display: flex;
            gap: 3rem;
        }
        
        .stat-item {
            text-align: left;
        }
        
        .stat-value {
            font-size: 2.5rem;
            font-weight: 800;
            background: linear-gradient(135deg, #fff 0%, #a78bfa 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .stat-label {
            color: rgba(255, 255, 255, 0.5);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* 3D Visual Container */
        .hero-visual {
            position: relative;
            height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            animation: fadeIn 1s ease forwards;
            animation-delay: 0.6s;
        }
        
        @keyframes fadeIn {
            to { opacity: 1; }
        }
        
        .floating-card {
            position: absolute;
            background: rgba(20, 20, 30, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 20px;
            padding: 20px;
            animation: floatCard 6s ease-in-out infinite;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }
        
        .floating-card-1 {
            top: 10%;
            left: 10%;
            animation-delay: 0s;
        }
        
        .floating-card-2 {
            top: 50%;
            right: 5%;
            animation-delay: -2s;
        }
        
        .floating-card-3 {
            bottom: 10%;
            left: 20%;
            animation-delay: -4s;
        }
        
        @keyframes floatCard {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-15px) rotate(2deg); }
            75% { transform: translateY(10px) rotate(-2deg); }
        }
        
        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 12px;
        }
        
        .card-title {
            font-weight: 600;
            margin-bottom: 4px;
        }
        
        .card-desc {
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Features Section */
        .features {
            padding: 100px 2rem;
            position: relative;
        }
        
        .section-container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }
        
        .section-header h2 {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }
        
        .section-header p {
            color: rgba(255, 255, 255, 0.6);
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
        }
        
        .feature-card {
            background: rgba(20, 20, 30, 0.6);
            border: 1px solid rgba(139, 92, 246, 0.1);
            border-radius: 24px;
            padding: 2rem;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        
        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #8b5cf6, transparent);
            opacity: 0;
            transition: opacity 0.3s;
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            border-color: rgba(139, 92, 246, 0.3);
            box-shadow: 0 30px 60px rgba(139, 92, 246, 0.15);
        }
        
        .feature-card:hover::before {
            opacity: 1;
        }
        
        .feature-icon {
            width: 70px;
            height: 70px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.2) 0%, rgba(236, 72, 153, 0.1) 100%);
            color: #a78bfa;
        }
        
        .feature-card h3 {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
        }
        
        .feature-card p {
            color: rgba(255, 255, 255, 0.5);
            line-height: 1.7;
        }
        
        /* Server Showcase - Marquee Style */
        .servers-showcase {
            padding: 80px 0;
            overflow: hidden;
        }
        
        .showcase-row {
            display: flex;
            gap: 1.5rem;
            padding: 1rem 0;
            animation: scrollLeft 30s linear infinite;
            width: max-content;
        }
        
        .showcase-row:hover {
            animation-play-state: paused;
        }
        
        .showcase-row.row-2 {
            animation: scrollRight 35s linear infinite;
            margin-top: 1.5rem;
        }
        
        @keyframes scrollLeft {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }
        
        @keyframes scrollRight {
            0% { transform: translateX(-50%); }
            100% { transform: translateX(0); }
        }
        
        .server-showcase-card {
            display: flex;
            align-items: center;
            gap: 12px;
            background: rgba(20, 20, 30, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 12px;
            padding: 12px 20px;
            min-width: 200px;
            transition: all 0.3s ease;
        }
        
        .server-showcase-card:hover {
            background: rgba(30, 30, 45, 0.8);
            border-color: rgba(139, 92, 246, 0.3);
            transform: translateY(-2px);
        }
        
        .server-icon {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            background: linear-gradient(135deg, #8b5cf6, #6366f1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            font-weight: 700;
            overflow: hidden;
            flex-shrink: 0;
        }
        
        .server-icon img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .server-info {
            display: flex;
            flex-direction: column;
            min-width: 0;
        }
        
        .server-name {
            font-size: 0.95rem;
            font-weight: 600;
            color: #fff;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            display: flex;
            align-items: center;
            gap: 6px;
        }
        
        .server-name .verified {
            color: #22c55e;
            font-size: 0.75rem;
        }
        
        .server-members {
            font-size: 0.8rem;
        }
        
        /* Integrations */
        .integrations {
            padding: 100px 2rem;
            background: linear-gradient(180deg, transparent 0%, rgba(139, 92, 246, 0.03) 50%, transparent 100%);
        }
        
        .integrations-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 1.5rem;
        }
        
        .integration-card {
            background: rgba(20, 20, 30, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s;
        }
        
        .integration-card:hover {
            transform: translateY(-8px) scale(1.02);
            border-color: rgba(139, 92, 246, 0.3);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }
        
        .integration-icon {
            width: 60px;
            height: 60px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.75rem;
            margin: 0 auto 1rem;
            transition: transform 0.3s;
        }
        
        .integration-card:hover .integration-icon {
            transform: scale(1.1) rotate(5deg);
        }
        
        .integration-card h4 {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .integration-card p {
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.5);
        }
        
        /* CTA Section */
        .cta {
            padding: 100px 2rem;
            text-align: center;
        }
        
        .cta-box {
            max-width: 800px;
            margin: 0 auto;
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(236, 72, 153, 0.1) 100%);
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 32px;
            padding: 4rem;
            position: relative;
            overflow: hidden;
        }
        
        .cta-box::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.1) 0%, transparent 50%);
            animation: rotateBg 20s linear infinite;
        }
        
        @keyframes rotateBg {
            to { transform: rotate(360deg); }
        }
        
        .cta-content {
            position: relative;
            z-index: 1;
        }
        
        .cta h2 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }
        
        .cta p {
            color: rgba(255, 255, 255, 0.6);
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }
        
        /* Footer */
        .footer {
            padding: 3rem 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            text-align: center;
            color: rgba(255, 255, 255, 0.4);
        }
        
        /* Mobile Menu */
        .mobile-menu-btn { display: none; background: none; border: none; color: #fff; font-size: 1.5rem; cursor: pointer; padding: 8px; }
        .mobile-menu { display: none; position: fixed; top: 76px; left: 0; right: 0; background: rgba(3, 0, 20, 0.98); backdrop-filter: blur(20px); padding: 1rem; border-bottom: 1px solid rgba(139, 92, 246, 0.1); z-index: 99; }
        .mobile-menu.open { display: block; }
        .mobile-menu a { display: block; padding: 1rem; color: rgba(255, 255, 255, 0.7); text-decoration: none; border-radius: 8px; }
        .mobile-menu a:hover { background: rgba(139, 92, 246, 0.1); color: #fff; }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .hero-container { grid-template-columns: 1fr !important; text-align: center !important; }
            .hero h1 { font-size: 3rem !important; }
            .hero p { max-width: 100% !important; margin-left: auto !important; margin-right: auto !important; }
            .hero-visual { display: none !important; visibility: hidden !important; }
            .hero-stats { justify-content: center !important; }
            .hero-content { align-items: center !important; }
            .features-grid { grid-template-columns: repeat(2, 1fr) !important; }
            .integrations-grid { grid-template-columns: repeat(2, 1fr) !important; }
            .nav-links { display: none !important; }
            .mobile-menu-btn { display: block !important; }
            #canvas-container, #canvas-container canvas, #canvas-container * { display: none !important; visibility: hidden !important; opacity: 0 !important; pointer-events: none !important; }
        }
        
        @media (max-width: 768px) {
            .nav { padding: 0.875rem 1rem !important; }
            .nav-brand span { font-size: 1.1rem !important; }
            .btn-glow { padding: 10px 16px !important; font-size: 0.875rem !important; }
            .hero { padding: 100px 1.25rem 50px !important; min-height: auto !important; }
            .hero-container { gap: 2rem !important; }
            .hero h1 { font-size: 2.25rem !important; }
            .hero p { font-size: 1rem !important; padding: 0 0.5rem !important; }
            .hero-badge { font-size: 0.8rem !important; padding: 6px 12px !important; }
            .hero-buttons { flex-direction: column !important; width: 100% !important; max-width: 320px !important; margin: 0 auto 2rem !important; }
            .btn-primary, .btn-secondary { width: 100% !important; justify-content: center !important; padding: 14px 20px !important; font-size: 1rem !important; }
            .hero-stats { gap: 2rem !important; flex-wrap: wrap !important; }
            .stat-value { font-size: 2rem !important; }
            .section-container { padding: 0 1rem !important; }
            .section-header h2 { font-size: 1.75rem !important; }
            .section-header p { font-size: 0.95rem !important; }
            .features { padding: 50px 1rem !important; }
            .features-grid { grid-template-columns: 1fr !important; gap: 1rem !important; }
            .feature-card { padding: 1.25rem !important; }
            .servers-showcase { padding: 50px 0 !important; }
            .showcase-row { gap: 0.75rem !important; }
            .server-showcase-card { min-width: 160px !important; padding: 10px 12px !important; }
            .server-icon { width: 32px !important; height: 32px !important; font-size: 1rem !important; }
            .server-name { font-size: 0.8rem !important; }
            .server-members { font-size: 0.7rem !important; }
            .integrations { padding: 50px 1rem !important; }
            .integrations-grid { grid-template-columns: repeat(2, 1fr) !important; gap: 0.75rem !important; }
            .integration-card { padding: 1rem !important; }
            .integration-icon { width: 50px !important; height: 50px !important; font-size: 1.5rem !important; margin-bottom: 0.75rem !important; }
            .cta { padding: 50px 1rem !important; }
            .cta-box { padding: 2rem 1.5rem !important; border-radius: 20px !important; }
            .cta h2 { font-size: 1.5rem !important; }
            .cta p { font-size: 0.95rem !important; }
            .orb { opacity: 0.15 !important; }
            .orb-1 { width: 300px !important; height: 300px !important; top: -150px !important; right: -150px !important; }
            .orb-2 { width: 250px !important; height: 250px !important; }
            .orb-3 { display: none !important; }
            #canvas-container, #canvas-container canvas, #canvas-container * { display: none !important; visibility: hidden !important; }
        }
        
        @media (max-width: 480px) {
            .nav { padding: 0.75rem !important; }
            .nav-brand img { width: 36px !important; height: 36px !important; }
            .nav-brand span { display: none !important; }
            .btn-glow .btn-text { display: none !important; }
            .btn-glow { padding: 10px 12px !important; }
            .hero { padding: 85px 1rem 40px !important; }
            .hero h1 { font-size: 1.875rem !important; line-height: 1.15 !important; margin-bottom: 1rem !important; }
            .hero p { font-size: 0.9rem !important; margin-bottom: 1.25rem !important; line-height: 1.6 !important; }
            .hero-badge { font-size: 0.7rem !important; margin-bottom: 0.875rem !important; padding: 5px 10px !important; }
            .hero-buttons { max-width: 280px !important; gap: 0.625rem !important; margin-bottom: 1.5rem !important; }
            .btn-primary, .btn-secondary { padding: 12px 16px !important; font-size: 0.9rem !important; border-radius: 12px !important; }
            .hero-stats { gap: 1.25rem !important; }
            .stat-item { text-align: center !important; }
            .stat-value { font-size: 1.625rem !important; }
            .stat-label { font-size: 0.7rem !important; }
            .section-header { margin-bottom: 1.5rem !important; }
            .section-header h2 { font-size: 1.375rem !important; }
            .section-header p { font-size: 0.85rem !important; }
            .features { padding: 40px 0.875rem !important; }
            .feature-card { padding: 1rem !important; border-radius: 16px !important; }
            .feature-icon { width: 48px !important; height: 48px !important; font-size: 1.25rem !important; margin-bottom: 1rem !important; border-radius: 14px !important; }
            .feature-card h3 { font-size: 1rem !important; margin-bottom: 0.5rem !important; }
            .feature-card p { font-size: 0.8rem !important; line-height: 1.5 !important; }
            .servers-showcase { padding: 40px 0 !important; }
            .integrations { padding: 40px 0.875rem !important; }
            .integrations-grid { gap: 0.625rem !important; }
            .integration-card { padding: 0.875rem !important; border-radius: 14px !important; }
            .integration-icon { width: 44px !important; height: 44px !important; font-size: 1.25rem !important; border-radius: 12px !important; }
            .integration-card h4 { font-size: 0.85rem !important; margin-bottom: 0.25rem !important; }
            .integration-card p { font-size: 0.7rem !important; }
            .cta { padding: 40px 0.875rem !important; }
            .cta-box { padding: 1.5rem 1.25rem !important; border-radius: 16px !important; }
            .cta h2 { font-size: 1.25rem !important; margin-bottom: 0.75rem !important; }
            .cta p { font-size: 0.85rem !important; margin-bottom: 1.25rem !important; }
            .cta .btn-primary { padding: 12px 20px !important; font-size: 0.9rem !important; }
            .footer { padding: 1.5rem 1rem !important; font-size: 0.8rem !important; }
            .orb { opacity: 0.1 !important; }
            .orb-1 { width: 200px !important; height: 200px !important; top: -100px !important; right: -100px !important; }
            .orb-2 { width: 150px !important; height: 150px !important; bottom: -75px !important; left: -75px !important; }
            .preloader-logo { width: 70px !important; height: 70px !important; border-radius: 18px !important; }
            .preloader-logo img { width: 50px !important; height: 50px !important; border-radius: 12px !important; }
            .preloader-text { font-size: 1.125rem !important; }
            .preloader-subtext { font-size: 0.8rem !important; margin-bottom: 1.5rem !important; }
            .preloader-bar { width: 160px !important; }
        }
        
        @media (max-width: 360px) {
            .hero h1 { font-size: 1.625rem !important; }
            .hero p { font-size: 0.85rem !important; }
            .hero-stats { gap: 1rem !important; }
            .stat-value { font-size: 1.5rem !important; }
            .btn-primary, .btn-secondary { padding: 11px 14px !important; font-size: 0.85rem !important; }
            .section-header h2 { font-size: 1.25rem !important; }
            .integrations-grid { grid-template-columns: 1fr !important; }
        }
        
        /* Scroll animations */
        .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Welcome Preloader -->
    <div class="welcome-preloader" id="welcomePreloader">
        <div class="preloader-logo">
            <img id="preloader-avatar" src="/images/bot-avatar.svg" alt="Bot">
        </div>
        <div class="preloader-text" id="preloader-name">Welcome</div>
        <div class="preloader-subtext">Loading amazing features...</div>
        <div class="preloader-bar">
            <div class="preloader-progress"></div>
        </div>
    </div>

    <!-- Background Elements -->
    <div class="grid-bg"></div>
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
    <div id="canvas-container"></div>

    <div class="content">
        <!-- Navigation -->
        <nav class="nav">
            <div class="nav-container">
                <a href="/" class="nav-brand">
                    <img id="bot-avatar" src="/images/bot-avatar.svg" alt="Bot">
                    <span id="bot-name">Dashboard</span>
                </a>
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="/commands">Commands</a>
                    <a href="/status">Status</a>
                    <a href="/docs">Docs</a>
                    <a href="/faq">FAQ</a>
                </div>
                <div style="display: flex; align-items: center; gap: 1rem;">
                    <% if (user) { %>
                        <a href="/dashboard" class="btn-glow">
                            <i class="fas fa-columns"></i> <span class="btn-text">Dashboard</span>
                        </a>
                    <% } else { %>
                        <a href="/auth/login" class="btn-glow">
                            <i class="fab fa-discord"></i> <span class="btn-text">Login</span>
                        </a>
                    <% } %>
                    <button class="mobile-menu-btn" onclick="toggleMobileMenu()"><i class="fas fa-bars"></i></button>
                </div>
            </div>
        </nav>
        
        <div class="mobile-menu" id="mobileMenu">
            <a href="/">Home</a>
            <a href="/commands">Commands</a>
            <a href="/status">Status</a>
            <a href="/docs">Docs</a>
            <a href="/faq">FAQ</a>
        </div>

        <!-- Hero Section -->
        <section class="hero">
            <div class="hero-container">
                <div class="hero-content">
                    <div class="hero-badge">
                        <i class="fas fa-circle" style="font-size: 8px; color: #22c55e;"></i>
                        <span>Online & Ready</span>
                    </div>
                    <h1>
                        The <span class="gradient-text">Ultimate</span><br>
                        Discord Bot
                    </h1>
                    <p>
                        Elevate your Discord server with powerful moderation, music, AI, and 50+ features. Trusted by thousands of communities worldwide.
                    </p>
                    <div class="hero-buttons">
                        <a href="/auth/login" class="btn-primary">
                            <i class="fas fa-plus"></i> Add to Discord
                        </a>
                        <a href="/vote" class="btn-secondary">
                            <i class="fas fa-star"></i> Vote for Us
                        </a>
                    </div>
                    <div class="hero-stats">
                        <div class="stat-item">
                            <div class="stat-value" id="guild-count">0</div>
                            <div class="stat-label">Servers</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value" id="user-count">0</div>
                            <div class="stat-label">Users</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value">99.9%</div>
                            <div class="stat-label">Uptime</div>
                        </div>
                    </div>
                </div>
                
                <div class="hero-visual">
                    <!-- Floating Feature Cards -->
                    <div class="floating-card floating-card-1">
                        <div class="card-icon" style="background: linear-gradient(135deg, #8b5cf6, #6366f1);">
                            <i class="fas fa-music"></i>
                        </div>
                        <div class="card-title">Music</div>
                        <div class="card-desc">High quality playback</div>
                    </div>
                    
                    <div class="floating-card floating-card-2">
                        <div class="card-icon" style="background: linear-gradient(135deg, #ec4899, #f43f5e);">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <div class="card-title">Moderation</div>
                        <div class="card-desc">Keep your server safe</div>
                    </div>
                    
                    <div class="floating-card floating-card-3">
                        <div class="card-icon" style="background: linear-gradient(135deg, #22c55e, #10b981);">
                            <i class="fas fa-brain"></i>
                        </div>
                        <div class="card-title">AI Powered</div>
                        <div class="card-desc">GPT-4 & Claude</div>
                    </div>
                    
                    <!-- 3D Bot Avatar -->
                    <div id="bot-3d" style="width: 300px; height: 300px;"></div>
                </div>
            </div>
        </section>

        <!-- Features Section -->
        <section class="features reveal">
            <div class="section-container">
                <div class="section-header">
                    <h2><span class="gradient-text">Powerful Features</span></h2>
                    <p>Everything you need to build and manage an amazing Discord community</p>
                </div>
                
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-brain"></i>
                        </div>
                        <h3>AI Assistant</h3>
                        <p>Chat with GPT-4, Claude, and Gemini. Generate images, summarize conversations, and more.</p>
                    </div>
                    
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-music"></i>
                        </div>
                        <h3>Music Player</h3>
                        <p>Crystal clear audio from YouTube, Spotify, SoundCloud with filters and 24/7 mode.</p>
                    </div>
                    
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <h3>Auto Moderation</h3>
                        <p>AI-powered content filtering, anti-spam, anti-raid, and comprehensive logging.</p>
                    </div>
                    
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-ticket-alt"></i>
                        </div>
                        <h3>Ticket System</h3>
                        <p>Professional support tickets with categories, transcripts, and claim system.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Server Showcase Section -->
        <section class="servers-showcase reveal">
            <div class="section-container">
                <div class="section-header">
                    <h2><span class="gradient-text">Trusted by Amazing Communities</span></h2>
                    <p id="showcase-subtitle">Join thousands of servers already using our bot</p>
                </div>
            </div>
            
            <!-- Row 1 - scrolls left -->
            <div class="showcase-row" id="showcaseRow1">
                <!-- Server cards will be loaded here -->
            </div>
            
            <!-- Row 2 - scrolls right -->
            <div class="showcase-row row-2" id="showcaseRow2">
                <!-- Server cards will be loaded here -->
            </div>
        </section>

        <!-- Integrations Section -->
        <section class="integrations reveal">
            <div class="section-container">
                <div class="section-header">
                    <h2><span class="gradient-text">Platform Integrations</span></h2>
                    <p>Connect with your favorite platforms seamlessly</p>
                </div>
                
                <div class="integrations-grid">
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #ff0000;">
                            <i class="fab fa-youtube"></i>
                        </div>
                        <h4>YouTube</h4>
                        <p>Music & notifications</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #1db954;">
                            <i class="fab fa-spotify"></i>
                        </div>
                        <h4>Spotify</h4>
                        <p>Playlists & tracks</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #9146ff;">
                            <i class="fab fa-twitch"></i>
                        </div>
                        <h4>Twitch</h4>
                        <p>Stream alerts</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: linear-gradient(45deg, #405de6, #c13584, #fd1d1d);">
                            <i class="fab fa-instagram"></i>
                        </div>
                        <h4>Instagram</h4>
                        <p>Post notifications</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #000; border: 1px solid rgba(255,255,255,0.2);">
                            <i class="fab fa-tiktok"></i>
                        </div>
                        <h4>TikTok</h4>
                        <p>Video feeds</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #000; border: 1px solid rgba(255,255,255,0.2);">
                            <i class="fab fa-x-twitter"></i>
                        </div>
                        <h4>Twitter</h4>
                        <p>Tweet tracking</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #ff4500;">
                            <i class="fab fa-reddit"></i>
                        </div>
                        <h4>Reddit</h4>
                        <p>Subreddit feeds</p>
                    </div>
                    
                    <div class="integration-card">
                        <div class="integration-icon" style="background: #ff5500;">
                            <i class="fab fa-soundcloud"></i>
                        </div>
                        <h4>SoundCloud</h4>
                        <p>Music streaming</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta reveal">
            <div class="cta-box">
                <div class="cta-content">
                    <h2>Ready to <span class="gradient-text">Transform</span> Your Server?</h2>
                    <p>Join thousands of communities already using our bot. Setup takes less than 2 minutes.</p>
                    <a href="/auth/login" class="btn-primary">
                        <i class="fab fa-discord"></i> Add to Discord — It's Free
                    </a>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <footer class="footer">
            <p id="footer-text">&copy; 2024 Discord Bot. All rights reserved.</p>
        </footer>
    </div>

    <script>
        // Three.js 3D Animation - Only run on desktop
        (function() {
            // Skip Three.js on mobile/tablet devices - check multiple conditions
            const isMobile = window.innerWidth <= 1024 || 
                             window.matchMedia('(hover: none)').matches ||
                             window.matchMedia('(pointer: coarse)').matches ||
                             'ontouchstart' in window ||
                             navigator.maxTouchPoints > 0;
            
            if (isMobile) {
                const container = document.getElementById('canvas-container');
                if (container) {
                    container.style.display = 'none';
                    container.style.visibility = 'hidden';
                    container.innerHTML = ''; // Remove any canvas elements
                }
                return;
            }
            
            const container = document.getElementById('canvas-container');
            const scene = new THREE.Scene();
            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });
            
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
            container.appendChild(renderer.domElement);
            
            // Create particles
            const particlesGeometry = new THREE.BufferGeometry();
            const particlesCount = 2000;
            const posArray = new Float32Array(particlesCount * 3);
            
            for (let i = 0; i < particlesCount * 3; i++) {
                posArray[i] = (Math.random() - 0.5) * 10;
            }
            
            particlesGeometry.setAttribute('position', new THREE.BufferAttribute(posArray, 3));
            
            const particlesMaterial = new THREE.PointsMaterial({
                size: 0.008,
                color: 0x8b5cf6,
                transparent: true,
                opacity: 0.8,
                blending: THREE.AdditiveBlending
            });
            
            const particlesMesh = new THREE.Points(particlesGeometry, particlesMaterial);
            scene.add(particlesMesh);
            
            // Create floating torus
            const torusGeometry = new THREE.TorusGeometry(1.5, 0.05, 16, 100);
            const torusMaterial = new THREE.MeshBasicMaterial({ 
                color: 0x8b5cf6,
                transparent: true,
                opacity: 0.3,
                wireframe: true
            });
            const torus = new THREE.Mesh(torusGeometry, torusMaterial);
            torus.position.set(2, 0, -2);
            scene.add(torus);
            
            // Create second torus
            const torus2 = new THREE.Mesh(
                new THREE.TorusGeometry(1, 0.03, 16, 100),
                new THREE.MeshBasicMaterial({ color: 0xec4899, transparent: true, opacity: 0.2, wireframe: true })
            );
            torus2.position.set(-2, 1, -3);
            torus2.rotation.x = Math.PI / 4;
            scene.add(torus2);
            
            camera.position.z = 3;
            
            // Mouse movement
            let mouseX = 0, mouseY = 0;
            document.addEventListener('mousemove', (e) => {
                mouseX = (e.clientX / window.innerWidth) * 2 - 1;
                mouseY = -(e.clientY / window.innerHeight) * 2 + 1;
            });
            
            // Animation loop
            function animate() {
                requestAnimationFrame(animate);
                
                particlesMesh.rotation.y += 0.0005;
                particlesMesh.rotation.x += 0.0002;
                
                torus.rotation.x += 0.005;
                torus.rotation.y += 0.003;
                
                torus2.rotation.x -= 0.003;
                torus2.rotation.z += 0.005;
                
                // Smooth camera movement
                camera.position.x += (mouseX * 0.5 - camera.position.x) * 0.02;
                camera.position.y += (mouseY * 0.3 - camera.position.y) * 0.02;
                camera.lookAt(scene.position);
                
                renderer.render(scene, camera);
            }
            
            animate();
            
            // Resize handler
            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });
        })();

        // Mobile menu toggle
        function toggleMobileMenu() {
            document.getElementById('mobileMenu').classList.toggle('open');
        }
        
        // Hide preloader after content loads
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('welcomePreloader').classList.add('hidden');
            }, 1800);
        });
        
        // Update preloader with bot info
        async function updatePreloader() {
            try {
                const response = await fetch('/api/bot-info');
                const data = await response.json();
                if (data.username) {
                    document.getElementById('preloader-name').textContent = `Welcome to ${data.username}`;
                }
                if (data.avatar) {
                    document.getElementById('preloader-avatar').src = data.avatar;
                }
            } catch (e) {}
        }
        updatePreloader();
        
        // Scroll reveal
        function revealOnScroll() {
            document.querySelectorAll('.reveal').forEach(el => {
                const top = el.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                if (top < windowHeight - 100) {
                    el.classList.add('active');
                }
            });
        }
        
        window.addEventListener('scroll', revealOnScroll);
        revealOnScroll();
        
        // Fetch bot info
        async function loadBotInfo() {
            try {
                const response = await fetch('/api/bot-info');
                const data = await response.json();
                
                if (data.username) {
                    document.getElementById('bot-name').textContent = data.username;
                    document.getElementById('page-title').textContent = `${data.username} - Home`;
                    document.getElementById('footer-text').textContent = `© 2024 ${data.username}. All rights reserved.`;
                }
                
                if (data.avatar) {
                    document.getElementById('bot-avatar').src = data.avatar;
                }
                
                // Animate stats
                animateValue('guild-count', 0, data.guilds || 0, 2000);
                animateValue('user-count', 0, data.users || 0, 2000);
            } catch (error) {
                console.error('Failed to load bot info:', error);
            }
        }
        
        function animateValue(id, start, end, duration) {
            const el = document.getElementById(id);
            const range = end - start;
            const startTime = performance.now();
            
            function update(currentTime) {
                const elapsed = currentTime - startTime;
                const progress = Math.min(elapsed / duration, 1);
                const easeProgress = 1 - Math.pow(1 - progress, 3);
                const current = Math.round(start + range * easeProgress);
                el.textContent = current.toLocaleString();
                
                if (progress < 1) {
                    requestAnimationFrame(update);
                }
            }
            
            requestAnimationFrame(update);
        }
        
        loadBotInfo();
        
        // Server Showcase - Marquee style with two rows
        async function loadServerShowcase() {
            try {
                const response = await fetch('/api/bot-info');
                const data = await response.json();
                
                // Update subtitle with real stats
                const subtitle = document.getElementById('showcase-subtitle');
                if (subtitle && data.guilds && data.users) {
                    subtitle.textContent = `Trusted by ${formatNumber(data.guilds)} servers, reaching ${formatNumber(data.users)} users`;
                }
                
                if (data.topGuilds && data.topGuilds.length > 0) {
                    renderMarquee(data.topGuilds);
                }
            } catch (error) {
                console.error('Failed to load server showcase:', error);
            }
        }
        
        function renderMarquee(guilds) {
            const row1 = document.getElementById('showcaseRow1');
            const row2 = document.getElementById('showcaseRow2');
            
            // Create card HTML
            const createCard = (guild) => `
                <div class="server-showcase-card">
                    <div class="server-icon">
                        ${guild.icon ? `<img src="${guild.icon}" alt="${escapeHtml(guild.name)}">` : guild.name.charAt(0)}
                    </div>
                    <div class="server-info">
                        <div class="server-name">
                            ${escapeHtml(guild.name)}
                            <i class="fas fa-check-circle verified"></i>
                        </div>
                        <div class="server-members">${formatNumber(guild.members)} members</div>
                    </div>
                </div>
            `;
            
            // Duplicate guilds for seamless loop
            const cardsHtml = guilds.map(createCard).join('');
            const duplicatedHtml = cardsHtml + cardsHtml; // Duplicate for infinite scroll
            
            // Split guilds between rows or use same for both
            row1.innerHTML = duplicatedHtml;
            row2.innerHTML = duplicatedHtml;
        }
        
        function formatNumber(num) {
            if (num >= 1000000) return (num / 1000000).toFixed(1) + 'M';
            if (num >= 1000) return (num / 1000).toFixed(1) + 'K';
            return num.toLocaleString();
        }
        
        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }
        
        loadServerShowcase();
    </script>
</body>
</html>
